## 1. Core resolver (`core/language.py`)

- [x] 1.1 Create `src/paca/core/language.py`: move `_LANG_NAMES`, `DEFAULT_LANG`/`DEFAULT_LANGUAGE`, `LANGUAGE_TOKEN`, `language_name()`, `language_rule()` out of `core/context.py` unchanged.
- [x] 1.2 Implement `_read_preference_file()` reading `STATE_ROOT / "language.json"`, returning `None` if absent, raising `RuntimeError` on an unrecognized `content_language` value.
- [x] 1.3 Implement `global_language()`: preference file if present, else the hardcoded `DEFAULT_LANGUAGE = "en"` constant. No `.env`/`SIGNAL_OUTPUT_LANG` read anywhere in this path.
- [x] 1.4 Implement `set_global_language(lang)`: validates against `_LANG_NAMES`, writes the preference file atomically (temp file + `os.replace`), including `updated_at`/`updated_by`.
- [x] 1.5 Implement `resolve_language(policy, *, override=None)` dispatching `off` / `global` / `same_as_source` (raises `RuntimeError` if `override` is `None`) / `fixed:<lang>`.
- [x] 1.6 Trim `core/context.py` back to `shared_context()`/`reload()` only; update its module docstring.
- [x] 1.7 Remove `OUTPUT_LANG_ENV`/`output_language()` (the env-var reader) entirely once nothing references it (task 2.x).

## 2. Loader wiring (`agents/loader.py`)

- [x] 2.1 Update `_compose_instructions` to read `cfg.extra.get("output_language", "global")`, normalize legacy `False` to `"off"`, and call `resolve_language(policy, override=...)`.
- [x] 2.2 Add an optional `language: str | None = None` parameter to `build_from_config`/`build_from_name`, threaded through as the `override`.
- [x] 2.3 Keep the existing "token in an opted-out prompt is a hard error" and "composed instructions still contain the token" guards working under the new resolver.
- [x] 2.4 Update imports across the codebase from `paca.core.context.{output_language,language_rule,...}` to `paca.core.language` (loader.py, cli.py's `paca doctor` check, `scripts/lang_probe.py`, `prompts/_shared/10_user_profile.md`).

## 3. Deterministic language detection (`core/language_detect.py`)

- [x] 3.1 Create `src/paca/core/language_detect.py` with `detect_language(text: str) -> str`, implementing the Unicode-script-ratio heuristic (CJK-range codepoints vs. total alphabetic codepoints, threshold-based `zh`/`en`).
- [x] 3.2 Tuned/validated the 0.15 threshold against real data during Docker verification: 38 real English titles pulled live via `paca info-radar pull` (0 misclassified as non-`en`) and 2 real Chinese titles generated by the live tier-2 run in task 9.4 (0 misclassified as non-`zh`). Small Chinese sample size — acceptable given the threshold's job (English-vs-Chinese script separation) is close to the ceiling for a heuristic; revisit if real ingested Chinese titles surface a miss.
- [x] 3.3 Write it as a single-purpose, swappable module (no call site imports internals beyond `detect_language`) so a future classifier swap is localized here.

## 4. info-radar: title through the LLM

- [x] 4.1 Add `title: str` to `Tier2Analysis` (`workflows/info_radar_analysis/schemas.py`).
- [x] 4.2 Update the tier-2 prompt (`prompts/agents/radar_tier2_impact.md`) to instruct the model to rewrite `item.title` into the target language, alongside the existing `summary`/`impact` instructions.
- [x] 4.3 Update `stages/tier2.py` to read `title` off the structured result. (No change needed — `stages/tier2.py` returns the whole `Tier2Analysis` object; `runner.py` reads `.title` off it.)
- [x] 4.4 Add a `title` column to `radar_analyses` in `scripts/bootstrap_db.py` (nullable, for existing rows). Added both to `CREATE TABLE` (fresh installs) and an idempotent `ALTER TABLE ADD COLUMN IF NOT EXISTS` (already-provisioned databases).
- [x] 4.5 Update `insert_analysis()` (`workflows/info_radar_analysis/store.py`) to accept and persist `title`; updated both `runner.py` call sites (duplicate + novel) to pass `analysis.title`.
- [x] 4.6 Update `dashboard/lib/radar/queries.ts` (`getDayGroups`, `getItemsForDay`, `getItemDetail`, `getFilteredTodayList`) to read `coalesce(ra.title, ri.title)`.
- [x] 4.7 Confirm `radar_tier1_filter`/`radar_tier2_impact`/`radar_recap` YAML configs keep `output_language` absent (defaults to `global`) — verified, no change needed.
- [x] 4.8 Set `radar_dedup_judge`'s `extra.output_language` to explicit `"off"` (was `false` — behaviorally identical via `normalize_policy`'s back-compat mapping, changed for clarity).

## 5. Knowledge ingestion: same-as-source

- [x] 5.1 Add a detection call in `stages/knowledge_ingest/fetch.py`'s top-level `fetch()`, run against `artifact.title` with fallback to `artifact.markdown` when no title exists, storing the result once on a new `KnowledgeArtifact.detected_language` field.
- [x] 5.2 Thread `artifact.detected_language` as `language=` through `_run_editor()` (`artifact_editor.py`) into `build_from_name("knowledge_artifact_editor"/"knowledge_github_cleaner", language=...)`.
- [x] 5.3 Thread the same field through `write_frontmatter()` into `build_from_name("knowledge_frontmatter"/"knowledge_github_summary", language=...)`.
- [x] 5.4 Update `configs/agents/knowledge_artifact_editor.yaml` and `knowledge_github_cleaner.yaml`: `extra.output_language: same_as_source` (was `false`).
- [x] 5.5 Update `configs/agents/knowledge_frontmatter.yaml` and `knowledge_github_summary.yaml`: `extra.output_language: same_as_source` (was absent/default `global`).
- [x] 5.6 Confirm the `{{OUTPUT_LANGUAGE}}` token substitution path still works unchanged for these four prompts under the new policy — verified: `_compose_instructions` doesn't distinguish policy sources, only the resolved `lang` value, which flows identically regardless of which policy produced it.
- [x] 5.7 Verify `_normalize_tags()` and the `tags` prompt clauses in `knowledge_frontmatter.md`/`knowledge_github_summary.md` are untouched by this change — confirmed, no edits made to either.

## 6. Dashboard sync

- [x] 6.1 Create `dashboard/instrumentation.ts` with a `register()` that creates `~/.next-signal/language.json` (seeded from `DEFAULT_LOCALE`) if it does not exist. Confirmed Next 15.5 needs no `experimental.instrumentationHook` flag.
- [x] 6.2 `LanguageToggle`'s cookie write turned out to be client-side (`document.cookie`, no pre-existing server action), not a server action as tasks.md assumed — added a new server action `setContentLanguage` (`dashboard/lib/actions/language.ts`) and called it from the same `onValueChange` handler that sets the cookie, rather than inventing a server action for the cookie write too (surgical fix, not a refactor of working code). Spec delta wording corrected to match.
- [x] 6.3 Confirmed: the existing `if (next === locale) return;` guard sits before both the cookie write and the new `setContentLanguage` call, so the no-op case still writes nothing.
- [x] 6.4 Verified — `dashboard: <<: *app` in `docker-compose.yml` inherits `PACA_STATE_DIR: /state` and the `pstate` volume from the shared anchor; added `stateRoot()`/`languageStateFile()` helpers to `dashboard/lib/paths.ts` mirroring the existing `PACA_STATE_DIR` fallback pattern already used in `lib/radar-ingest.ts`.

## 7. Deployment and docs cleanup

- [x] 7.1 Remove `SIGNAL_OUTPUT_LANG` from `.env.example`.
- [x] 7.2 Update bilingual docs (`docs/` + `docs/zh/`): `modules/core.md`, `development.md`, `operations.md` (env var list, "where state lives", `paca doctor` description) — all four pairs. Also updated `CLAUDE.md` (Chinese-only by the project's own convention, no mirror needed).
- [x] 7.3 Update `docs/modules/info_filter.md` and `docs/modules/knowledge.md` (+ zh mirrors) for the `title` field and the `same_as_source` switch respectively — including narrowing the knowledge.md re-index/rename hazard note, since `same_as_source` makes it no longer operator-triggerable project-wide.

## 8. Tests

- [x] 8.1 Unit tests for `detect_language()` (`tests/test_language_detect.py`, new): deterministic across repeated calls, correct `zh`/`en` classification on representative samples (including title-length inputs and mixed-language robustness), no network/model calls.
- [x] 8.2 Unit tests for `resolve_language()` (`tests/test_language.py`, new, replacing the output-language half of `test_context.py`): all four policies, `same_as_source`-without-override `RuntimeError`, unrecognized-preference-file-value `RuntimeError`, corrupt-file `RuntimeError`, atomic-write round-trip, call-time (not cached) resolution.
- [x] 8.3 Unit tests for the preference file read/write path — folded into 8.2 (same module).
- [x] 8.4 Updated `tests/test_agent_loader.py`: new `_compose` helper patches `paca.core.language.global_language` at the source rather than the value, exercising the real policy dispatch; added coverage for `same_as_source` (override wins, ignores global, raises without one) and `fixed:<lang>`; removed two tests whose premise (`global` resolving to `None`) no longer exists under the new design.
- [x] 8.5 Full suite green (`uv run pytest -q`: 396 passed, 14 skipped — all skips are pre-existing `@pytest.mark.integration`/DB-gated). Fixed incidental breakage surfaced by the schema/signature changes: `Tier2Analysis(...)` now requires `title` (4 call sites across `test_info_radar_analysis_tier2.py`/`_runner.py`); `artifact_editor_mod.build_from_name` is now called with a `language=` kwarg, so every test-double lambda mocking it needed the new parameter (`test_knowledge_artifact_editor.py`, `test_knowledge_artifacts.py`, `test_knowledge_ingest_workflow.py`); added a `radar_analyses.title` persistence integration test and two tests asserting `detected_language` threads correctly into both the cleaning and frontmatter agent builds.

## 9. Verification (Docker, per `docker-verify` skill)

- [x] 9.1 `docker compose build` — succeeded (both `dashboard` and `bootstrap` targets), then `docker compose up -d`; confirmed `docker image inspect next-signal:latest` and `docker inspect next-signal-dashboard-1 --format {{.Image}}` return the same ID (container running current code, not stale).
- [x] 9.2 `paca doctor` inside the container: exits 1 (expected under the cloud-only profile — no `OMLX_BASE_URL`/`ANTHROPIC_API_KEY`), but the "content language" line reports `en (from /state/language.json)` on first boot, correctly reflecting the file `instrumentation.ts` seeded on container start. Manually overwrote the file to `zh` and re-ran doctor — it picked up `zh` immediately, confirming call-time (not cached) resolution.
- [x] 9.3 `paca knowledge ingest --no-ingest https://github.com/octocat/Hello-World` with the live preference file deliberately left at `zh` (from the doctor check above) — the strongest possible test of `same_as_source` independence. Result: `summary` came back in English, `title` stayed `octocat/Hello-World` (the repo identifier, untranslated), `tags` are lowercase English (`markdown`, `demo`, `tutorial`), and the cleaned wiki markdown body is English throughout — verified both in the CLI's JSON output and by reading the written `/wiki/temp-inbox/octocat-Hello-World.md` directly. Global preference had zero effect, as designed. (Only tested one source, English under a conflicting `zh` global setting — the mechanism is symmetric and unconditional by construction, so a Chinese source was not additionally run.)
- [x] 9.4 `paca info-radar pull` (8 real items) then `paca info-radar analyze --limit 7` with the global preference still at `zh` — 2 items reached tier-2 successfully (1 tier2_error, consistent with known DeepSeek-fallback structured-output flakiness, not title-specific — see design.md risks). Queried `radar_analyses` directly: both rows have `title`/`tier1_reason`/`impact_md` consistently in Chinese, proper nouns (`GPT-5.6`, `OpenAI`, `GPT-Realtime`) preserved untranslated, `tags` lowercase English. No item left with a translated summary/impact but an untranslated title.
- [x] 9.5 Full stack was already up throughout (`docker compose up -d`, all 6 dashboard routes swept 200 OK). Verified the mechanism the toggle depends on end-to-end (file write → immediate pickup by both `paca doctor` and a real `paca info-radar analyze` run, above) rather than driving the browser UI itself — no browser-automation tool was available in this session to click the actual `LanguageToggle` control. The toggle's `onValueChange` calls the same `setContentLanguage` action verified here, so this is the same code path, just not exercised via a real click.
