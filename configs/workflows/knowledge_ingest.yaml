name: knowledge_ingest
kind: workflow
enabled: true
description: Fetch, edit, classify, persist, and optionally index one knowledge item.
factory: next_signal.workflows.knowledge_ingest:build
expose:
  agent_os: true
  tool:
    enabled: true
    name: knowledge_ingest_workflow
inputs_schema:
  value: string
  ingest: boolean
extra:
  run_now: next_signal.workflows.knowledge_ingest:run
  tool_fn: next_signal.workflows.knowledge_ingest:ingest_one
