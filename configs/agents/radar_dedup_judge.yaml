name: radar_dedup_judge
model_profile: local_structured
tools: []
instructions_file: agents/radar_dedup_judge.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # Only `is_duplicate` / `matched_topic_id` are consumed; `reason` is discarded
  # (radar_analyses has no column for it and nothing renders it), so a language
  # rule here would be prompt cost with no user-visible effect.
  output_language: false
