name: radar_tier2_impact
model_profile: local_structured
tools: []
instructions_file: agents/radar_tier2_impact.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # title/summary/impact are all user-facing prose on the analysis card, so
  # they need the language rule. `global` is already the loader's default for
  # a missing key -- declared explicitly here so that's visible without
  # reading paca.agents.loader.
  output_language: global
