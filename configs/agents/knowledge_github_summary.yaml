name: knowledge_github_summary
model_profile: local
tools: []
instructions_file: agents/knowledge_github_summary.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # summary is the repo's index entry — same rationale as knowledge_frontmatter:
  # generated prose for the reader follows the content-language setting, while
  # knowledge_github_cleaner keeps the README body in its own language.
  # `title` stays the `owner/repo` string regardless.
  output_language: global
