name: knowledge_artifact_editor
model_profile: local
tools: []
instructions_file: agents/knowledge_artifact_editor.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # Output is the article body itself — translating it destroys the only copy
  # of the source text the wiki holds. Targets the source's own detected
  # language explicitly, never the operator's global preference (spec:
  # core-output-language, knowledge-pipeline).
  output_language: same_as_source
