name: radar_tier1_filter
model_profile: local_structured
tools: []
instructions_file: agents/radar_tier1_filter.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # `reason` is persisted to radar_analyses.tier1_reason and rendered on the
  # item detail page, so it needs the language rule. `global` is already the
  # loader's default for a missing key -- declared explicitly here so that's
  # visible without reading next_signal.agents.loader.
  output_language: global
