name: radar_recap
model_profile: local_structured
tools: []
instructions_file: agents/radar_recap.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # headline/theme narrative are user-facing prose in the recap view, so they
  # need the language rule. `global` is already the loader's default for a
  # missing key -- declared explicitly here so that's visible without reading
  # next_signal.agents.loader.
  output_language: global
