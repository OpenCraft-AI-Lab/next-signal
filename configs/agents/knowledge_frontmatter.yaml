name: knowledge_frontmatter
model_profile: local
tools: []
instructions_file: agents/knowledge_frontmatter.md
markdown: false
add_history_to_context: false
add_datetime_to_context: false
extra:
  shared_context: false
  db: false
  # title/summary are the artifact's index entry — what the knowledge list and
  # review cards display — so they follow the reader's content-language
  # setting, not the source. The archive is preserved by the body, which
  # `knowledge_artifact_editor` keeps in its own language (spec:
  # core-output-language, knowledge-pipeline).
  output_language: global
