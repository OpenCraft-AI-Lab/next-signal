# User profile

Edit this file to give every agent context about who they're serving. Kept
minimal here so the file is safe to commit; for sensitive personal facts,
keep a `99_private.md` in the same dir (git-ignored — see `.gitignore`).

- Single user, technically literate, comfortable with code and CLI.
- Working language: English (writes English by default; sometimes Chinese).
- Operating timezone: America/Los_Angeles.
- Typical workflows: software engineering, info-radar review,
  knowledge-base curation.
